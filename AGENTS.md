# Agent工作约束

## 防止重复输出

- 同一句话、同一动作描述不得连续输出两次。
- 每完成一个有实际进展的步骤后，只发送一次简短进度更新。
- 不要在每次工具调用前都重复“我现在要做……”。

## 防止重复重试

- 同一工具调用连续失败三次后，停止重试，改为检查日志、文件状态或直接向用户报告阻塞原因。
- 重试前必须先确认上一次操作是否已经完成，避免重复执行已成功执行的步骤。
- 如果错误信息相同且没有新的解决方案，不得继续重复相同命令。

## 输出纪律

- 发现回复开始重复相同词语时，立即停止生成并输出一次简短状态说明。
- 优先复用已经检查过的结果，不要为了“确认”而反复读取相同内容。
- 所有用户可见消息保持简洁，不输出无意义的过程复述。
