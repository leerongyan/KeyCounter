# 键盘鼠标使用统计工具：开发计划

## 1. 项目目标

开发一个在 Windows 后台运行的本地软件，自动记录：

- 每个键盘按键的使用次数
- 鼠标左键、右键、中键、滚轮的使用次数
- 鼠标移动距离
- 产生键盘热力图和鼠标使用报告

软件只统计按键名称和使用次数，不记录输入文字内容，也不会上传任何数据。

## 2. 最终功能清单

### 核心功能

- 统计每个按键的按压次数，支持按天、按小时、按日期范围查看
- 键盘热力图：使用越多的键显示越红、越亮
- 统计鼠标点击次数和滚轮次数
- 统计鼠标移动距离（像素和大致米数）
- 数据保存在本地，重启软件后不丢失
- 暂停 / 继续统计
- 系统托盘常驻，可一键打开统计面板
- 导出 CSV 数据和热力图 PNG 图片

### 暂不实现

- 记录输入内容（文字、密码、聊天内容）
- 云端同步、多设备统计
- 识别人工智能输入法、自动补全等复杂场景

## 3. 任务拆解

整个软件可以分成 6 个模块：

```text
数据采集 -> 数据存储 -> 统计计算 -> 可视化 -> 用户界面 -> 打包发布
```

### 3.1 数据采集模块

- 监听键盘按下和松开事件
- 只保存按键代码，例如 `A`、`Enter`、`Shift`
- 监听鼠标点击、滚轮和移动事件
- 记录事件发生的时间

### 3.2 数据存储模块

- 使用 SQLite 数据库保存数据
- 按天聚合统计数据，避免数据文件无限变大
- 每天自动生成汇总表

### 3.3 统计计算模块

- 统计每个按键的总次数、当天次数
- 统计鼠标各按键次数
- 计算鼠标移动距离
- 计算每天输入量、最常用按键、点击峰值时段

### 3.4 可视化模块

- 键盘热力图：在键盘布局图上按频率叠加颜色
- 鼠标点击热力图：把屏幕分成网格，显示点击密集区域
- 折线图：显示每天 / 每小时使用量变化
- 排行榜：显示最常用的 10 个按键

### 3.5 用户界面模块

- 系统托盘小图标，右键可暂停、继续、退出
- 本地网页仪表盘，浏览器打开即可查看
- 实时刷新当日数据

### 3.6 打包发布模块

- 把 Python 程序打包成单个 `.exe` 文件
- 在无 Python 环境的电脑上也能运行

## 4. 技术选型

适合零基础学习者的一套方案：

| 用途 | 推荐工具 | 原因 |
| --- | --- | --- |
| 编程语言 | Python 3.12 | 语法简单，生态丰富 |
| 键盘鼠标监听 | pynput | 官方库，适合全局统计 |
| 数据存储 | SQLite | Python 自带，无需安装服务器 |
| 网页仪表盘 | Flask + HTML/CSS/JS | 界面美观，热力图容易实现 |
| 图表 | ECharts 或 Chart.js | 现成图表库，不用手写 |
| 打包 | PyInstaller | 一键生成 exe |
| 代码编辑 | VS Code | 免费、插件多 |

如果希望桌面窗口更简单，也可以先用 Python 自带的 Tkinter，但热力图效果不如网页方案。

## 5. 数据结构设计

先使用最简单的表结构：

### 按键事件表 `key_events`

- `id`：事件编号
- `key_name`：按键名称，例如 `A`、`Enter`
- `pressed_at`：按下时间

### 鼠标事件表 `mouse_events`

- `id`：事件编号
- `event_type`：左键、右键、中键、滚轮
- `x`、`y`：事件发生时鼠标坐标
- `pressed_at`：发生时间

### 移动采样表 `mouse_moves`

- `id`：事件编号
- `x`、`y`：采样点坐标
- `sampled_at`：采样时间
- `distance_px`：与上一个采样点的距离

### 每日汇总表 `daily_stats`

- `date`：日期
- `total_keys`：当天按键总数
- `total_clicks`：当天点击总数
- `total_distance_px`：当天移动距离
- `active_minutes`：当天活跃分钟数

## 6. 分阶段开发计划

以下计划按每周投入 5-10 小时估算，总共约 6-10 周。

### 阶段 0：环境准备（3-5 天）

- 安装 Python 3.12
- 安装 VS Code
- 学会新建文件、运行 Python 脚本
- 学会最基础的 Git 使用

验收标准：可以运行一个 `hello.py` 并打印文字。

### 阶段 1：采集 MVP（1-2 周）

- 安装并使用 pynput
- 后台监听键盘、鼠标
- 在控制台实时显示按键总次数
- 按 `Esc` 退出程序

验收标准：连续按 10 次 `A`，程序显示 `A: 10`；程序内不保存任何输入文字。

### 阶段 2：数据存储（1 周）

- 学习 SQLite 基础
- 把事件写入数据库
- 按天生成汇总数据

验收标准：关闭程序再打开，昨天的统计数据仍然存在。

### 阶段 3：键盘热力图（1-2 周）

- 先制作标准 104 键位键盘布局
- 把每个按键映射到布局上的坐标
- 根据使用次数给按键叠加颜色
- 生成 PNG 图片或网页热力图

验收标准：`A` 按 100 次、`Z` 按 10 次时，`A` 明显更红。

### 阶段 4：鼠标统计（1 周）

- 统计左键、右键、中键、滚轮次数
- 计算鼠标移动距离
- 忽略手部轻微抖动产生的微小移动
- 可选：制作屏幕点击热力图

验收标准：把鼠标匀速移动约 1 米，统计结果接近预期距离。

### 阶段 5：仪表盘界面（1-2 周）

- 用 Flask 启动本地网页
- 显示当天按键次数、点击次数、移动距离
- 显示按键排行榜和热力图
- 支持暂停 / 继续统计

验收标准：打开浏览器输入本地地址，可以看到实时更新的数据。

### 阶段 6：打磨和发布（1-2 周）

- 系统托盘图标
- 开机自启动
- 数据导出
- 打包成 exe
- 编写使用说明书

验收标准：双击 exe 即可使用，不要求用户安装 Python。

## 7. 核心难点和应对方案

### 7.1 全局键盘监听会被安全软件拦截

应对：先把功能做出来，最后打包时处理杀毒软件误报问题；开发阶段直接运行 Python 即可。

### 7.2 键盘布局和热力图坐标映射

应对：先支持标准 104 键美式布局；键盘按键名称本身不依赖中文或英文输入法，热力图可以正常显示字母键。中文输入法下的符号键映射放在后期优化。

### 7.3 隐私安全

应对：软件只记录按键名称和次数，绝不保存按键顺序、输入内容、窗口标题或剪贴板。键盘统计虽然在技术原理上属于按键记录，但本软件的设计目标不是窃取信息；不要在共享电脑或公司电脑上未经允许运行。

### 7.4 鼠标距离不稳定

应对：鼠标事件到达频率很高，不能每条都算距离。采样间隔限制为每 20-50 毫秒一次，并过滤小于 2 像素的抖动，这样统计结果更稳定。

### 7.5 多显示器和缩放比例

应对：第一版只统计主屏幕，后续再处理多显示器坐标换算。

## 8. 零基础学习路线

1. 先学 Python 基础：变量、列表、字典、循环、函数，够用即可
2. 跟着 pynput 官方文档做第一个监听程序
3. 用到 SQLite 时再学 SQLite 基础
4. 用到网页时再学一点 HTML、CSS、JavaScript
5. 每个阶段都先做出可运行的小版本，再逐步完善

不需要提前学完所有理论。开发计划本身就是学习路线，每个阶段只学当前需要的知识。

## 9. 建议的开发顺序

```text
阶段 0 环境准备
  -> 阶段 1 采集 MVP
  -> 阶段 2 数据存储
  -> 阶段 3 键盘热力图
  -> 阶段 4 鼠标统计
  -> 阶段 5 仪表盘界面
  -> 阶段 6 打包发布
```

每次完成一个阶段后先运行验收，再进入下一个阶段。
