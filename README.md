# 键盘鼠标使用统计工具

一个在 Windows 本地运行的统计工具：

- 统计每个键盘按键的使用次数
- 生成键盘热力图
- 统计鼠标左键、右键、中键、滚轮次数
- 统计鼠标移动距离
- 通过本地网页查看实时数据

## 如何运行

1. 双击项目目录中的 `start.bat`
2. 程序会自动打开浏览器，地址是 `http://127.0.0.1:8765`
3. 点击页面右上角按钮可以暂停或继续统计
4. 在黑色命令行窗口按 `Ctrl+C` 可以停止程序

也可以手动运行：

```powershell
cd D:\codex_project\Key_counter
python app.py
```

## 数据保存在哪里

统计数据保存在项目目录下的 `data\keycounter.db` 文件中。

## 隐私说明

本软件只记录按键名称和次数，例如 `A`、`Enter`、`Ctrl`，不会保存输入的文字内容、按键顺序、窗口标题或剪贴板内容。数据只保存在本地，不会上传。

## 当前进度

- 已完成：按键采集、鼠标采集、SQLite 存储、键盘热力图、鼠标距离统计、本地网页仪表盘
- 待完成：系统托盘常驻、开机自启、数据导出、打包成 exe
